# EFT SIP Calculator — состояние проекта

> Актуально на 26 сентября 2026 года. Этот файл — основная точка входа для
> продолжения разработки, проверки production и передачи проекта в другой чат.
> Он описывает фактическое состояние `main`, а не только первоначальный замысел.

## 1. Краткий статус

| Параметр | Текущее состояние |
| --- | --- |
| Репозиторий | `svc4p7vc8y-beep/EFT-SIP-Calculator` |
| Основная ветка | `main` |
| Текущий commit | `e3d6ca3` — `Release v149 fix mail sending and folders` |
| Версия интерфейса | `149` (`REACT_PROJECT_VERSION`) |
| Версия npm-пакета | `0.19.0` — техническая версия пакета, не номер релиза UI |
| Синхронизация Git | локальный `main` и `origin/main`: `0/0` на момент проверки |
| Production-калькулятор | <https://calc.eftsip.ru/> |
| Публичный сайт | <https://eftsip.ru/> |
| Публичная анкета | <https://eftsip.ru/EFT_client_questionnaire.html> |
| API | <https://eftsip.ru/api/api.php> |
| Хостинг | Beget, PHP 7.4, MySQL, SSH/rsync из GitHub Actions |
| Почтовый ящик приложения | `sale@eftsip.ru` через защищённые IMAP/SMTP-настройки сервера |
| Последняя проверка URL | калькулятор, сайт, анкета и публичная сессия API отвечают HTTP 200 |
| Последний CI/CD | четыре workflow для commit `e3d6ca3` завершились успешно |
| Автотесты последнего релиза | 286 тестов; итог актуальной локальной проверки см. раздел 12 |

Секреты, пароли, ключи БД и почтовый пароль в репозитории не хранятся. Они
передаются через GitHub Actions Secrets и создают закрытые файлы конфигурации на
сервере.

## 2. Карта системы

```text
Клиент сайта
   │
   ├── eftsip.ru ── публичный каталог домов и форма «Обсудить проект»
   │                    │
   └── публичная анкета ┤
                        ▼
               eftsip.ru/api/api.php
                  │             │
                  │             ├── уведомление через sale@eftsip.ru
                  │             └── вложения анкеты
                  ▼
              MySQL на Beget
                  ▲
                  │ личная сессия сотрудника + CSRF
                  │
             calc.eftsip.ru
        внутренний калькулятор сотрудников
```

Внутренний калькулятор и публичный сайт собираются из одного репозитория, но это
разные Vite-сборки и разные точки публикации. Клиент публичного сайта не получает
доступ к калькулятору: на `calc.eftsip.ru` требуется индивидуальная учётная запись
сотрудника.

## 3. Реализовано в калькуляторе

### 3.1. План дома

- прямоугольные и многоугольные помещения;
- отдельные стены, перегородки, разрывы стен, окна, двери и гаражные ворота;
- террасы и крыльца со ступенями, сваями, обвязкой и кровлей;
- свайные ряды, отдельные сваи и автоматическая равномерная раскладка;
- перемещение комнат после выбора, редактирование узлов и привязка к геометрии;
- пользовательские подписи и стрелки, перемещение подписей комнат;
- размеры с выносом, настройка размера текста;
- отмена/повтор, выбор пробелом, полноэкранный режим, масштабирование колесом и
  панорамирование средней кнопкой мыши;
- отдельные видимые слои плана, свай, обвязки, кровли и размеров;
- работа с первым и вторым этажом;
- построение кровли по фактическому контуру, включая Г-образный дом;
- отдельные схемы свайного поля и кровли для печатной сметы;
- защита тяжёлых экранов `ErrorBoundary`, чтобы ошибка одного раздела не давала
  полностью пустую страницу.

### 3.2. Конструктив и расчёты

- сваи и трёхслойная обвязка;
- SIP-пол, межэтажное перекрытие, стены, потолок и перегородки;
- семьи SIP-панелей и толщины 124/174/224 мм;
- двускатная, вальмовая, плоская односкатная и контурная кровля;
- фронтоны по типу кровли, стропильные системы и кровли пристроек;
- холодная, тёплая и комбинированная крыша;
- стандартный или усиленный каркас тёплой SIP-кровли;
- кровельные покрытия и возможность исключить покрытие, сохранив SIP-каркас,
  крепёж и раскрой;
- окна, панорамное остекление, входные двери, двери с терморазрывом, секционные и
  роллетные ворота по фактическим размерам;
- инженерия: электрика, водоснабжение, канализация, вентиляция и отопление;
- тёплый пол, гидроизоляция, сетка, труба, хомуты и изменяемая полусухая стяжка;
- внешняя и внутренняя отделка, доставка;
- расчёт пеноклея и крепежа по узлам;
- отдельная редактируемая ведомость «Метизы по узлам», включая массу саморезов,
  резерв, упаковки и печатный сводный заказ;
- конструкционный крепёж пола, потолка, стен, проёмов и сборной обвязки без
  ранее обнаруженного двойного счёта узла 8×320.

Правила расчётов находятся прежде всего в `src/react/calculations/`. Изменять
формулы, нормы и цены следует только вместе с регрессионным тестом и пояснением
источника по правилам `AGENTS.md`.

### 3.3. Смета, прайс и документы

- редактируемый прайс материалов и работ;
- индикатор изменения цен и кнопка возврата к исходному каталогу;
- экранная, печатная и Excel-смета;
- независимые переключатели работ, комплектующих, планов и печатных слоёв;
- раскрой не исчезает при отключении обычных работ;
- дата документа берётся на момент формирования/создания проекта;
- планы берутся из фактического «Плана дома»;
- собственные изображения в смете: до 8 изображений и суммарно до 2,8 МБ;
- ручная «Заявка»: внутренний документ или коммерческое предложение, выбор строк
  из прайса, ручные цена и количество, печать и Excel;
- справочник SIP, библиотека знаний, схемы отопления и технологические материалы.

### 3.4. Стандарт жилого дома

Есть команда «Жилой дом — стандарт EFT». Она сохраняет геометрию, проёмы,
проектные цены и ручные правки, но применяет типовой конструктив. Сейчас в коде
подтверждены:

- пол и межэтажное перекрытие 224 мм, усиленная раскладка 625 мм;
- стены 174 мм, потолок 174 мм;
- клеёный пакет досок;
- равномерные сваи и синхронная обвязка;
- двускатная холодная кровля, автоматические стропила 50×150;
- высота конька 1,8 м, карнизный свес 0,5 м;
- профиль покрытия сохраняется из текущего проекта, а не принудительно меняется.

Незавершённое расхождение с последним согласованным набором пользователя:

- торцевой свес в пресете сейчас 0,3 м, требовалось 0,5 м;
- шаг стропил сейчас 0,6 м, в запросе отдельно назывался шаг обрешётки 0,5 м;
- шаг обрешётки базовой модели сейчас 0,35 м;
- карнизные и торцевые планки и уплотнитель конька базово включены, хотя в
  последнем запросе их требовалось выключить;
- пресет включает основные SIP-разделы, но не приводит каждый переключатель
  состава расчёта к явно перечисленному пользователем состоянию;
- сечение 50×100 присутствует в каталоге/параметрах проекта, однако не является
  отдельным значением, принудительно задаваемым этим пресетом.

До отдельного исправления этот пункт нельзя считать полностью завершённым.

## 4. Совместная работа сотрудников

В production включён командный режим:

- индивидуальный вход сотрудника вместо общего Basic Auth;
- роли `admin`, `manager`, `estimator`, `viewer`;
- администратор создаёт и редактирует сотрудников и меняет им пароль;
- общие проекты хранятся в MySQL;
- новый общий проект сразу получает единый порядковый номер;
- сохраняются текущая ревизия и контрольные версии проекта;
- сервер не позволяет молча записать старую ревизию поверх более новой;
- для удаления проекта или анкеты требуется повторный ввод пароля;
- список анкет, статусы, непрочитанные заявки и импорт анкеты в проект;
- общие библиотека планов и библиотека знаний;
- журнал ключевых серверных действий подготовлен как база для будущей CRM;
- локальное автосохранение браузера осталось аварийной копией, но не считается
  главным общим хранилищем.

Интервалы автоматического обновления клиента: общие проекты и анкеты — примерно
45 секунд, статус почты — примерно 60 секунд. Это polling, а не мгновенные
WebSocket-уведомления.

## 5. Анкета и публичные заявки

- анкета является отдельной публичной страницей и не открывает клиенту
  калькулятор;
- обязательный телефон вводится по российскому шаблону и передаётся без подмены
  на тестовый номер;
- поддерживается несколько вложений/изображений;
- заявке присваивается общий последовательный номер;
- анкета сохраняется в MySQL, видна сотрудникам и может быть импортирована в
  проект;
- заявка с карточки дома также сохраняется в закрытом рабочем пространстве;
- сервер пытается отправить уведомление на активный командный ящик
  `sale@eftsip.ru`;
- если уведомление не ушло, сохранённая заявка не теряется и остаётся в
  калькуляторе.

Геометрия из анкеты является исходными данными для менеджера, а не автоматически
утверждённым конструктивным проектом.

## 6. Почта

В калькулятор встроен серверный почтовый клиент для `sale@eftsip.ru`:

- IMAP-папки берутся непосредственно с сервера;
- поддерживаются «Входящие», «Отправленные», «Черновики», «Архив», «Спам»,
  «Корзина» и пользовательские папки;
- показываются общее число писем и непрочитанные;
- чтение писем и скачивание вложений;
- новое письмо, ответ и привязка письма к проекту;
- до 8 вложений в письмо, один файл до 8 МБ, общий объём до 20 МБ;
- черновик сохраняется в реальную IMAP-папку;
- успешно отправленное SMTP-письмо копируется в «Отправленные»;
- ошибка отправки показывается внутри окна письма, окно не закрывается;
- после успешной отправки окно закрывается и список обновляется.

В v149 исправлена несовместимость с PHP 7.4 (`str_starts_with` заменён на
совместимую проверку). Автоматические тесты, сборка и UI-сценарии ошибки/успеха
пройдены. После v149 не выполнялась реальная отправка внешнему получателю из
production под учётной записью сотрудника; это остаётся короткой ручной
приёмочной проверкой.

## 7. Общее файловое хранилище

Раздел «Файлы» реализован на сервере Beget и доступен только сотрудникам:

- вложенные папки;
- множественная загрузка;
- документы, таблицы, презентации, изображения, архивы, JSON, DWG и DXF;
- один файл до 8 МБ;
- одна загрузка: до 10 файлов и до 20 МБ;
- виртуальная папка «Файлы из анкет» собирает вложения анкет без дублирования;
- содержимое хранится в MySQL (`MEDIUMBLOB`).

VK WorkSpace Disk напрямую к калькулятору не подключён. Отдельного внешнего
объектного хранилища, квот, очистки старых файлов и автоматического вывоза
резервной копии файлов вне Beget пока нет. При росте объёма следует вынести
бинарные файлы в S3-совместимое хранилище, а в MySQL оставить метаданные и права.

## 8. Серверная база данных

Схема находится в `server/beget-api/schema.sql`.

| Таблица | Назначение |
| --- | --- |
| `eft_users` | сотрудники, роли, парольные хэши, активность |
| `eft_projects` | текущие общие проекты и ревизии |
| `eft_project_versions` | история контрольных версий |
| `eft_questionnaires` | анкеты и заявки |
| `eft_questionnaire_attachments` | вложения анкет |
| `eft_intake_reads` | прочтение анкет конкретными сотрудниками |
| `eft_shared_documents` | общая библиотека планов и знаний |
| `eft_audit_log` | журнал ключевых действий |
| `eft_counters` | атомарная порядковая нумерация |
| `eft_mail_links` | связь писем с проектами |
| `eft_file_folders` | дерево общего файлового раздела |
| `eft_files` | файлы и содержимое общего хранилища |

Миграции идемпотентно выполняются защищённым `migrate.php` через отдельный
workflow. Файл `config.local.php` не должен попадать в Git или перезаписываться
обычной публикацией.

## 9. Безопасность и доступ

- HTTPS активен на публичном сайте и калькуляторе;
- доступ к калькулятору контролируется личной серверной сессией сотрудника;
- пароли хранятся в виде `password_hash`, а не открытым текстом;
- изменяющие API-запросы сотрудников защищены CSRF-токеном;
- удаление важных сущностей требует повторной проверки пароля;
- публичные отправки ограничены по частоте;
- почтовый пароль существует только в GitHub Secret и закрытом серверном файле;
- SSH known hosts проверяется строго;
- публикация не удаляет посторонние серверные файлы и исключает секретные
  конфиги;
- `.htaccess`/резерв прежней Basic Auth сохранялись при переходе на личный вход.

Не публиковать в issue, чат, commit или этот файл значения GitHub Secrets,
пароли сотрудников, пароль приложения почты и реквизиты MySQL.

## 10. Автоматическая публикация

| Workflow | Назначение | Автозапуск |
| --- | --- | --- |
| `beget.yml` | тест, сборка и публикация калькулятора на `calc.eftsip.ru` | изменения калькулятора в `main` |
| `beget-site.yml` | тест, сборка сайта/анкеты, PHP lint и публикация на `eftsip.ru` | изменения сайта, анкеты или API |
| `beget-api-migrate.yml` | загрузка API и защищённая миграция MySQL | изменения серверного API/схемы |
| `pages.yml` | публичная сборка на GitHub Pages | каждый push в `main` |
| `beget-mail-configure.yml` | проверка IMAP/SMTP и установка закрытого почтового конфига | вручную/при изменении workflow |
| `beget-calculator-auth-cutover.yml` | резерв и переход с общего Basic Auth на личный вход | служебный, повторять без причины не нужно |
| `beget-site-stage.yml` | безопасная предварительная выкладка публичного сайта | вручную |
| `beget-site-check.yml` | read-only проверка корня публичного сайта | вручную |

Текущий production развернут из `e3d6ca3`. Для этого commit успешны:

1. `Deploy EFT calculator to Beget`;
2. `Deploy EFT public site to Beget`;
3. `Migrate EFT shared database`;
4. `Deploy EFT public site to GitHub Pages`.

## 11. Хронология основной работы этого чата

### Версии 121–123

- завершён расчёт конструкционных саморезов;
- создана многошаговая анкета будущего дома;
- добавлены уклон плоской кровли и расчёт/рисование фронтонов;
- исправлены боковая навигация и отображение параметров разных крыш;
- настроена первая безопасная публикация калькулятора на Beget.

### Публичный сайт и версии 124–132

- опубликованы сайт и расширенная анкета;
- калькулятор возвращён на корень `calc.eftsip.ru` после ошибочной подмены;
- добавлены узловая ведомость крепежа, расчёт проёмов по размеру, заявка/КП;
- добавлен стандарт жилого дома и индикатор изменённых цен;
- развиты выбор/перемещение на плане, кровли пристроек, Г-образная кровля и
  стропильные схемы террас;
- в смету добавлены актуальная дата, планы и раздельные листы.

### Версии 130–136: командная работа

- MySQL API, личные сотрудники, роли и общие проекты;
- хранение ревизий и общих библиотек;
- анкеты, вложения, уведомления и импорт в проект;
- последовательные номера заявок и проектов;
- защищённые удаления;
- командный почтовый клиент;
- устойчивое создание/сохранение новых проектов и сохранение текущих цен.

### Версии 137–144

- новые проекты домов и планы на публичном сайте;
- SMTP-уведомления публичных заявок;
- обязательный форматированный телефон;
- более понятные узлы на плане;
- отопление и тёплый пол;
- редактирование сотрудников администратором;
- изображения и реальные планы в смете;
- исправление пустых экранов «Параметры»/«Кровля» и узких экранов.

### Версии 145–149

- переход почты и уведомлений на `sale@eftsip.ru`;
- полноэкранный план, масштабирование и панорамирование;
- уточнённый расчёт крепежа по узлам и перевод количества саморезов в массу;
- тёплая SIP-кровля и корректная логика отключения покрытия/работ;
- общий файловый менеджер;
- вложения в исходящие письма;
- реальные IMAP-папки, серверные черновики и копирование исходящих;
- исправлена отправка multipart-писем на PHP 7.4.

Подробности последних релизов: `docs/RELEASE_147.md`,
`docs/RELEASE_148.md`, `docs/RELEASE_149.md`.

## 12. Проверка качества

Обязательная команда перед релизом:

```powershell
npm ci --no-audit --no-fund
npm test
npm run build
npm run build:public
```

На v149 ранее подтверждены:

- 286 автоматических тестов;
- production-сборка калькулятора;
- desktop и mobile UI-проверка почты на ширине 390 px;
- сценарий ошибки SMTP: сообщение видно, форма остаётся открытой;
- сценарий успеха: форма закрывается, появляется уведомление;
- live-проверка версии и защищённого API.

Актуальная проверка после создания этого документа 26 сентября 2026 года:

- `npm test`: **286/286 тестов пройдены**;
- `npm run build`: **успешно**, production-сборка калькулятора создана;
- `npm run build:public`: **успешно**, production-сборка сайта и анкеты создана.

## 13. Известные ограничения и незавершённые задачи

Высокий приоритет:

1. Привести пресет «Жилой дом — стандарт EFT» к последнему согласованному
   составу и добавить точные регрессионные проверки всех переключателей.
2. Выполнить одну контролируемую реальную отправку письма с небольшим вложением
   из production под учётной записью сотрудника и проверить «Отправленные».
3. Добавить регулярный внешний резерв MySQL и файлов, не зависящий только от
   резервных копий Beget.

Средний приоритет:

4. Определить политику квот и архивирования общего файлового хранилища.
5. Добавить мгновенные уведомления (SSE/WebSocket или отдельный push-механизм),
   если polling 45–60 секунд недостаточен.
6. Спроектировать отдельную CRM на отдельном поддомене. Текущий `eft_audit_log`,
   проекты, анкеты и почтовые связи являются подготовленной базой, но полноценной
   CRM, календаря, управления производством и монтажными бригадами пока нет.
7. Решить, нужен ли внешний диск VK WorkSpace или S3. VK Disk сейчас не
   интегрирован и его веб-интерфейс нельзя считать API для калькулятора.
8. Обновить `README.md` и старые handoff-документы: README всё ещё описывает
   ранний React v47, а часть старых текстов противоречит production v149.

Технический долг:

- `PlanScreen.jsx` и `Calculators.jsx` очень велики; новые функции следует
  выносить в небольшие компоненты без изменения расчётной логики;
- release-теги после `v129` не создавались, хотя commits имеют версии 130–149;
- npm-версия `0.19.0` не синхронизирована с пользовательским номером релиза;
- нет автоматического end-to-end теста реального IMAP/SMTP без секретов;
- нет отдельной панели просмотра/восстановления `eft_audit_log`.

## 14. Состояние локальной рабочей копии

Каноническая актуальная копия на машине на момент подготовки этого файла:

```text
C:\Users\alfav\Desktop\ПРОЕКТ\-EFT-SIP-Calculator-main
```

В ней есть пользовательские **untracked** материалы, не входящие в commit
`e3d6ca3`: handoff-архивы, черновики инструкций, рендеры документов, копии анкеты,
несколько изображений и `docs/CRM_HANDOFF.md`. Они не анализируются как часть
production и не должны удаляться или добавляться в Git массово без отдельной
проверки.

Служебный worktree
`C:\Users\alfav\.codex\worktrees\6a73\-EFT-SIP-Calculator-main` находится на
старом detached commit `d72588d` и содержит untracked `beget.yml` и
`BEGET_DEPLOYMENT.md`. Его нельзя использовать для следующего релиза и нельзя
принудительно переносить поверх актуального `main`.

Сам `PROJECT_STATUS.md` создан как новый отслеживаемый кандидат, но до отдельной
команды пользователя не коммитится и не публикуется.

## 15. Где искать код

```text
src/react/app/                 оболочка, навигация, lazy-экраны
src/react/screens/             план, параметры, расчёты, файлы, почта, смета
src/react/calculations/        расчётное ядро и узлы
src/react/state/               модель проекта, миграции, пресеты, история
src/react/planner/             геометрия и взаимодействия плана
src/react/data/                каталоги и справочники
src/react/export/              Excel-экспорт
src/react/cloud/               командная сессия и синхронизация
src/questionnaire/             публичная анкета
src/site/                      публичный сайт
src/shared/                    общий API-клиент и телефонный формат
server/beget-api/              PHP API, почта, схема и миграции MySQL
tests/                         34 файла Node test
.github/workflows/             публикация и служебные операции Beget/Pages
docs/                          релизные и предметные документы
```

## 16. Безопасный порядок продолжения

1. Работать только из актуального `main`; сначала `git fetch` и сравнить ветки.
2. Проверить `git status` и не трогать чужие untracked-файлы.
3. Прочитать `AGENTS.md` и этот статус.
4. Найти действующую формулу/экран и все его потребители.
5. Для цены или нормы показать старое значение, источник, новое значение и
   влияние на смету до изменения.
6. Сохранить совместимость старых `.eft.json` через безопасные значения и
   миграцию.
7. Добавить регрессионные тесты.
8. Запустить полный набор тестов и обе сборки.
9. Выполнить визуальную проверку desktop/mobile для изменённого интерфейса.
10. Коммитить и публиковать в `main` только по прямой команде пользователя.
